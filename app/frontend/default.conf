# Fallback only - see the ConfigMap-mounted config for the real one used in k8s.
server {
    listen 8080;
    server_name _;

    location / {
        return 503 "vmapp-frontend: no backend configured (standalone container)\n";
    }
}
